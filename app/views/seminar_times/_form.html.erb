<%= form_for(@seminar_time) do |f| %>
  <% if @seminar_time.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@seminar_time.errors.count, "error") %> prohibited this seminar_time from being saved:</h2>

      <ul>
      <% @seminar_time.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start_date %><br />
    <%= f.datetime_select :start_date %>
  </div>
  <div class="field">
    <%= f.label :end_date %><br />
    <%= f.datetime_select :end_date %>
  </div>
  <div class="field">
    <%= f.label :seminar_id %><br />
    <%= f.number_field :seminar_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
