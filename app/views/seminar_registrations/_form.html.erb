<%= form_for(@seminar_registration) do |f| %>
  <% if @seminar_registration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@seminar_registration.errors.count, "error") %> prohibited this seminar_registration from being saved:</h2>

      <ul>
      <% @seminar_registration.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :seminar_time_id %><br />
    <%= f.number_field :seminar_time_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
